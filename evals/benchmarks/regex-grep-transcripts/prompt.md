Output only a single extended regular expression, with no prose.

The regex should match transcript summary lines that contain `tool=Bash` and `class=git`, and should not match lines for other tools.
